<Application x:Class="Tiny_GymBook.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="using:Microsoft.UI.Xaml.Controls"
             xmlns:utu="using:Uno.Toolkit.UI"
             xmlns:utum="using:Uno.Toolkit.UI.Material">



  <Application.Resources>
    <ResourceDictionary>

      <!-- 1) WinUI Controls Resources (muss zuerst) -->
      <ResourceDictionary.MergedDictionaries>
        <controls:XamlControlsResources/>

        <!-- 2) Uno Toolkit Material Theme -->
        <utum:MaterialToolkitTheme
          ColorOverrideSource="ms-appx:///Styles/ColorPaletteOverride.xaml">
          <!--
          Optional: eigenes Font-Set überschreiben
          <utum:MaterialToolkitTheme.FontOverrideDictionary>
            <ResourceDictionary>
              <FontFamily x:Key="MaterialLightFontFamily">ms-appx:///Uno.Fonts.Roboto/Fonts/Roboto-Light.ttf#Roboto</FontFamily>
              <FontFamily x:Key="MaterialMediumFontFamily">ms-appx:///Uno.Fonts.Roboto/Fonts/Roboto-Medium.ttf#Roboto</FontFamily>
              <FontFamily x:Key="MaterialRegularFontFamily">ms-appx:///Uno.Fonts.Roboto/Fonts/Roboto-Regular.ttf#Roboto</FontFamily>
            </ResourceDictionary>
          </utum:MaterialToolkitTheme.FontOverrideDictionary>
          -->
        </utum:MaterialToolkitTheme>

        <!-- 3) Uno Toolkit (Controls/Styles wie Card etc.) -->
        <utu:ToolkitResources/>
      </ResourceDictionary.MergedDictionaries>


      <!-- Globaler Icon-Button-Style -->
      <Style x:Key="IconCircleButton"
             TargetType="Button">
        <Setter Property="MinWidth"
                Value="44"/>
        <Setter Property="MinHeight"
                Value="44"/>
        <Setter Property="Padding"
                Value="0"/>
        <Setter Property="CornerRadius"
                Value="22"/>
        <Setter Property="HorizontalAlignment"
                Value="Center"/>
        <Setter Property="VerticalAlignment"
                Value="Center"/>
        <Setter Property="Background"
                Value="{ThemeResource ControlFillColorDefaultBrush}"/>
        <Setter Property="BorderBrush"
                Value="{ThemeResource ControlStrokeColorDefaultBrush}"/>
        <Setter Property="BorderThickness"
                Value="1"/>
        <Setter Property="ContentTemplate">
          <Setter.Value>
            <!-- DataContext = Button.Content -> Glyph="{Binding}" -->
            <DataTemplate>
              <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}"
                        Glyph="{Binding}"/>
            </DataTemplate>
          </Setter.Value>
        </Setter>
      </Style>




      <!-- Eigene globale Ressourcen hier einfügen -->

      <!-- Global verfügbare Höhe zum „TabBar-Spacer“ -->
      <x:Double x:Key="TabBarHeight">56</x:Double>



    </ResourceDictionary>
  </Application.Resources>
</Application>
