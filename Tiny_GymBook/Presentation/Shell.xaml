<UserControl x:Class="Tiny_GymBook.Presentation.Shell"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:Tiny_GymBook.Presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:regions="using:Uno.Extensions.Navigation.Regions"
      xmlns:utu="using:Uno.Toolkit.UI"
      mc:Ignorable="d"
      d:DesignHeight="300"
      d:DesignWidth="400">

    <Grid>
        <!-- SplashScreen oder was immer du willst -->
        <utu:ExtendedSplashScreen x:Name="Splash"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            HorizontalContentAlignment="Stretch"
            VerticalContentAlignment="Stretch">
            <!-- ... wie gehabt ... -->
        </utu:ExtendedSplashScreen>

        <!-- Das ist die eigentliche "Region" für dein Content! -->
        <ContentControl regions:Region.Name="MainRegion"/>
    </Grid>
</UserControl>
